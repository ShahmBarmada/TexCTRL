import type { MetaFunction } from "@remix-run/node";

export const meta: MetaFunction = () => {
  return [
    { title: "TexCTRL Login" },
    { name: "TexCTRL Mokhmal" },
  ];
};

export default function LoginPage() {
  return (
    <div>

    </div>
  );
}
